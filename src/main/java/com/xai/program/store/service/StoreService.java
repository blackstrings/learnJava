package com.xai.program.store.service;

/**
 * Services are meant to handle logics that doesn't fit anywhere else
 * and especially when the logic doesn't fit in any domain object.
 * It is best to keep domain logic inside the domain.
 *
 */
public class StoreService {

}
